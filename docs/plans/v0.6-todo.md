# v0.6 Execution TODO

按 M1 顺序执行。每完成一项 = `cargo build` + `cargo test` + 端到端冒烟。

## BashRunner F001 (P0)
- [ ] 看 `bash_runner.rs` 当前接口
- [ ] 实现 `run(cmd, cwd) -> BashResult`，用 `std::process::Command`
- [ ] 加 timeout 字段
- [ ] stderr 区分
- [ ] BashExecution 加 stderr
- [ ] `rich.rs` 渲染 stderr
- [ ] 单元测试 + e2e 测试
- [ ] `cargo test` 跑通

## FileCompletion F002 (P0)
- [ ] walkdir 加进 Cargo.toml
- [ ] `extract_at_prefix_pair` 真实现
- [ ] `search_files_as_struct` walkdir + 过滤 + 缓存
- [ ] `completion.rs` (state.rs) 加 file completion popup 区分
- [ ] 单元测试
- [ ] `cargo test`

## Ctrl+L F003 (P2 but plan says P0, fixing anyway)
- [ ] `runtime.rs:843` 改用 open_selector 路径
- [ ] 删 "switch model (not yet implemented)" 字符串
- [ ] 验证 `/model` 与 Ctrl+L 都开 selector

## StatusBar F004 (P0)
- [ ] `nini-core` 加 `git_branch(cwd)` 函数
- [ ] `cli/main.rs` init callback 调 `state.set_cwd_and_git`
- [ ] `runtime.rs` 处理 Usage 事件写 `state.tokens` + `state.cost_usd`
- [ ] model context_window 加载
- [ ] e2e 验证状态栏

## 4 missing commands F005 (P0)
- [ ] `commands.rs` 加 `CommandId::Help/Debug/Status/Editor`
- [ ] REGISTRY 加条目
- [ ] dispatch 各实现
- [ ] `/editor` 调 $VISUAL/$EDITOR

## /resume F006 (P0)
- [ ] `session_entries()` 改 walkdir
- [ ] cwd 优先排序
- [ ] 限 50 + 过滤提示

## Completion Scroll F007 (P1)
- [ ] `CompletionPopup` 加 scroll_offset
- [ ] Up/Down 滚动
- [ ] `render_completion_popup` 加 indicator

## Single-Enter F008 (P1)
- [ ] `KeyAction::Submit` smart 分支
- [ ] popup hint

## Selector Layout F009 (P1)
- [ ] `render_selector_panel` rect 重算
- [ ] input 区强制保留

## Final
- [ ] tmux 端到端验证
- [ ] CHANGELOG
- [ ] commit + PR-ready